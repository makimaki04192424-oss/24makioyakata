Please add your hero image here with the filename `hero.jpg` or `hero.jpeg`.

Recommended sizes:
- Desktop: 1600×900 (or wider), optimized and compressed to ~200–400KB
- Mobile: can be same image, CSS will crop/cover to fit

Name the file `hero.jpg` or `hero.jpeg` (or update `styles.css` background url if you pick another name) and refresh your local server to see the change.